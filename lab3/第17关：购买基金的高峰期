-- 17 查询2022年2月购买基金的高峰期。至少连续三个交易日，所有投资者购买基金的总金额超过100万(含)，则称这段连续交易日为投资者购买基金的高峰期。只有交易日才能购买基金,但不能保证每个交易日都有投资者购买基金。2022年春节假期之后的第1个交易日为2月7日,周六和周日是非交易日，其余均为交易日。请列出高峰时段的日期和当日基金的总购买金额，按日期顺序排序。总购买金额命名为total_amount。
--    请用一条SQL语句实现该查询：

select pro_purchase_time, total_amount as amount
from(
    select pro_purchase_time,total_amount,rn
    from(
        select pro_purchase_time,sum(f_amount*pro_quantity) as total_amount,(rank() over (order by property.pro_purchase_time)) as rn
        from property,fund
        where pro_type=3 and pro_pif_id=f_id
        group by pro_purchase_time
    )prf1
    where total_amount>=100000
)
where rn in(
    select rn
    from(
        select pro_purchase_time,sum(f_amount*pro_quantity) as total_amount,(rank() over (order by property.pro_purchase_time)) as rn
        from property,fund
        where pro_type=3 and pro_pif_id=f_id
        group by pro_purchase_time
    )prf1
    where total_amount>=100000
)
and
rn in(
    select rn+1
    from(
        select pro_purchase_time,sum(f_amount*pro_quantity) as total_amount,(rank() over (order by property.pro_purchase_time)) as rn
        from property,fund
        where pro_type=3 and pro_pif_id=f_id
        group by pro_purchase_time
    )prf1
    where total_amount>=100000
)
and 
rn in(
    select rn-1
    from(
        select pro_purchase_time,sum(f_amount*pro_quantity) as total_amount,(rank() over (order by property.pro_purchase_time)) as rn
        from property,fund
        where pro_type=3 and pro_pif_id=f_id
        group by pro_purchase_time
    )prf1
    where total_amount>=100000
)
or
rn in(
    select rn
    from(
        select pro_purchase_time,sum(f_amount*pro_quantity) as total_amount,(rank() over (order by property.pro_purchase_time)) as rn
        from property,fund
        where pro_type=3 and pro_pif_id=f_id
        group by pro_purchase_time
    )prf1
    where total_amount>=100000
)
and
rn in(
    select rn+1
    from(
        select pro_purchase_time,sum(f_amount*pro_quantity) as total_amount,(rank() over (order by property.pro_purchase_time)) as rn
        from property,fund
        where pro_type=3 and pro_pif_id=f_id
        group by pro_purchase_time
    )prf1
    where total_amount>=100000
)
and 
rn in(
    select rn+2
    from(
        select pro_purchase_time,sum(f_amount*pro_quantity) as total_amount,(rank() over (order by property.pro_purchase_time)) as rn
        from property,fund
        where pro_type=3 and pro_pif_id=f_id
        group by pro_purchase_time
    )prf1
    where total_amount>=100000
)
or
rn in(
    select rn
    from(
        select pro_purchase_time,sum(f_amount*pro_quantity) as total_amount,(rank() over (order by property.pro_purchase_time)) as rn
        from property,fund
        where pro_type=3 and pro_pif_id=f_id
        group by pro_purchase_time
    )prf1
    where total_amount>=100000
)
and
rn in(
    select rn-2
    from(
        select pro_purchase_time,sum(f_amount*pro_quantity) as total_amount,(rank() over (order by property.pro_purchase_time)) as rn
        from property,fund
        where pro_type=3 and pro_pif_id=f_id
        group by pro_purchase_time
    )prf1
    where total_amount>=100000
)
and 
rn in(
    select rn-1
    from(
        select pro_purchase_time,sum(f_amount*pro_quantity) as total_amount,(rank() over (order by property.pro_purchase_time)) as rn
        from property,fund
        where pro_type=3 and pro_pif_id=f_id
        group by pro_purchase_time
    )prf1
    where total_amount>=100000
)


/*  end  of  your code  */
